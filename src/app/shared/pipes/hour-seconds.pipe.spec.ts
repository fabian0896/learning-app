import { HourSecondsPipe } from './hour-seconds.pipe';

describe('HourSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new HourSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
