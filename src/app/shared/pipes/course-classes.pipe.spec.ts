import { CourseClassesPipe } from './course-classes.pipe';

describe('CourseClassesPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseClassesPipe();
    expect(pipe).toBeTruthy();
  });
});
