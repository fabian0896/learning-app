import { CourseHoursPipe } from './course-hours.pipe';

describe('CourseHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
